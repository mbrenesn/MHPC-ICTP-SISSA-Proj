function loschmidt = hardcorebosons( ham_mat, times, v_initial, tolerance )

v_help = v_initial;
sizeits = size(times);
iterations = sizeits(2);

for tt=2:iterations
    w = expv(times(tt)-times(tt-1),ham_mat,v_he,1e-4,30)

end

